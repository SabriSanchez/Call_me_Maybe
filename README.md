# Call_me_Maybe
Function-calling engine for Large Language Models (LLMs) that bridges natural language and executable functions. Uses constrained decoding to enforce predefined schemas, reducing hallucinations and ensuring structured, type-safe outputs.
